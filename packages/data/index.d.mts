/** Contributor-edited curation folder (absolute filesystem path). */
export const curationDir: string;
/** Generated MDT seed JSON (absolute filesystem path). */
export const spellSeedPath: string;
/** Generated DB2 mechanical facts JSON (absolute filesystem path). */
export const spellFactsPath: string;
/** Generated per-dungeon enemy facts JSON for pull detection (absolute filesystem path). */
export const dungeonEnemiesPath: string;
/** Generated mechanics bundle JSON — the single client-served artifact (absolute filesystem path). */
export const mechanicsPath: string;
/** Hand-curated overlay JSON (absolute filesystem path). */
export const overlayPath: string;
/** Removal-category vocabulary JSON (absolute filesystem path). */
export const removalCategoriesPath: string;
/** Player removers JSON (absolute filesystem path). */
export const removersPath: string;
/** Dangerous player-debuffs JSON, grouped by dungeon (absolute filesystem path). */
export const debuffsPath: string;
/** Mythic+ affix metadata and fallback display names JSON (absolute filesystem path). */
export const affixesPath: string;
/** Consolidated mechanic CARDS folder — one file per dungeon (M+) / encounter (raid). */
export const mechanicsCardsDir: string;
