{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capability for the main MythicIQ window: core events (run-carved), the folder picker, and OS notifications. App-defined commands (start_watching, etc.) are allowed without an explicit ACL entry.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "notification:default",
    "opener:allow-open-url",
    "opener:allow-default-urls",
    "fs:allow-mkdir",
    "fs:allow-read-dir",
    "fs:allow-read-file",
    "fs:allow-write-file",
    "fs:allow-read-text-file",
    "fs:allow-write-text-file",
    "fs:allow-remove",
    "fs:allow-exists"
  ]
}
